<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="qna.model.Qna">

	<select id="GetAllData" resultType="MyQna">
		select * from qna
		order by num desc
	</select>

	<select id="GetTotalCount" resultType="int">
		select count(*) from qna
	</select>

	<insert id="InsertData">
		insert into
		qna(num,writer,cate1,cate2,cate3,subject,content,reg_date,ref,re_step,re_level,reply)
		values(q_seq.nextval,#{writer},#{cate1},#{cate2},#{cate3},#{subject},
		#{content,jdbcType=VARCHAR},sysdate,#{q_seq.currval},0,0,'no') 
	</insert>
	
	
</mapper>
  

 <!-- #{reg_date} -->